// Configuration object for the Bubbles game
const config = {
    // Telegram bot token (replace with your own from @BotFather)
    BOT_TOKEN: '8080569643:AAGWqdS_a2J340wcRNZK-GE-DZeA4D3UZso',
    // Telegram channel username (replace with your own, without @)
    CHANNEL_USERNAME: 'bubblesgame_bot',
    // Web app URL (replace with your deployed webapp URL)
    WEBAPP_URL: 'https://vrtesikkk.github.io/Bubblesgame/' // Replace with your actual domain
};

// Prevent modification of the config object
Object.freeze(config); 